/*  const users = require('./users');*/
const links = require('./links');
/*const array = [
  users,
  links
  ]
const pgp = (param) => { 
  require(param)
};

for (const key in array) {
pgp(key)
}

module.exports = () => {
  for (const key in array) {
    pgp(key)
  };
} 
  */
module.exports = links;

